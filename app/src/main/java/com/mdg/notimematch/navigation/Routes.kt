package com.mdg.notimematch.navigation

enum class Routes(val value: String) {
    HOME(value = "home"),
    CLOSET(value = "closet"),
    CAMERA(value = "camera"),
    CONFIRM_PHOTO(value = "confirmPhoto")
}